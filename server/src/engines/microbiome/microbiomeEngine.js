// Microbiome & Disease Engine
export const PATHOGEN_TYPES={intestinal_parasite:{transmission:'fecal_oral',base_mortality:0.05,duration_days:30,immunity_duration:365*3,density_threshold:3},cholera_like:{transmission:'water',base_mortality:0.3,duration_days:7,immunity_duration:365*5,density_threshold:5},respiratory_common:{transmission:'airborne',base_mortality:0.02,duration_days:14,immunity_duration:180,density_threshold:2},pneumonia_like:{transmission:'airborne',base_mortality:0.15,duration_days:21,immunity_duration:365*2,density_threshold:4},plague_like:{transmission:'airborne',base_mortality:0.4,duration_days:10,immunity_duration:365*10,density_threshold:8},malaria_like:{transmission:'vector',base_mortality:0.1,duration_days:14,immunity_duration:365,density_threshold:1,biomes:['tropical','coastal']},fever_tick:{transmission:'vector',base_mortality:0.08,duration_days:10,immunity_duration:365*2,density_threshold:1,biomes:['grassland','temperate_forest']},wound_infection:{transmission:'contact',base_mortality:0.12,duration_days:14,immunity_duration:60,density_threshold:1},fungal_skin:{transmission:'contact',base_mortality:0.01,duration_days:30,immunity_duration:180,density_threshold:2}};
export function processMicrobiomeTick(population,worldState,simDay){const events=[];const density=population.filter(i=>!i.is_dead).length;for(const[pathId,path]of Object.entries(PATHOGEN_TYPES)){if(density<path.density_threshold)continue;if(path.biomes&&!path.biomes.includes(worldState.biome))continue;const sm=(worldState.season==='summer'&&path.transmission==='vector')?2.0:(worldState.season==='winter'&&path.transmission==='airborne')?1.5:1.0;if(Math.random()<density*0.0001*sm){const ob=triggerOB(pathId,path,population,simDay);if(ob.affected>0)events.push(ob.event);}}for(const ind of population){if(!ind.infections||ind.is_dead)continue;for(const inf of[...ind.infections]){inf.days_remaining--;const path=PATHOGEN_TYPES[inf.pathogen_id];if(!path)continue;if(Math.random()<path.base_mortality*(1-(ind.phenotype.immune_strength??0.5)*0.7)*(1-(ind.health_score??0.5)*0.3)/path.duration_days){ind.is_dead=true;ind.cause_of_death=`disease_${inf.pathogen_id}`;ind.death_day=simDay;}if(inf.days_remaining<=0||ind.is_dead){ind.infections=ind.infections.filter(i=>i!==inf);if(!ind.is_dead){if(!ind.immunities)ind.immunities={};ind.immunities[inf.pathogen_id]=simDay+path.immunity_duration;}}}}return events;}
function triggerOB(pathId,path,population,simDay){const s=population.filter(i=>!i.is_dead&&!i.infections?.some(inf=>inf.pathogen_id===pathId)&&!(i.immunities?.[pathId]>simDay));if(s.length===0)return{affected:0};const infected=s.slice(0,1+Math.floor(Math.random()*2));for(const ind of infected){if(!ind.infections)ind.infections=[];ind.infections.push({pathogen_id:pathId,days_remaining:path.duration_days,infected_day:simDay});}return{affected:infected.length,event:{type:'epidemic_outbreak',pathogen_id:pathId,initial_cases:infected.length,day:simDay,importance:path.base_mortality>0.2?'high':'medium',description:`A ${pathId.replace(/_/g,' ')} outbreak begins`}};}
export function spreadInfection(infected,susceptible,pathId,simDay){if(!infected.infections?.some(i=>i.pathogen_id===pathId)||susceptible.immunities?.[pathId]>simDay)return false;const path=PATHOGEN_TYPES[pathId];if(!path)return false;const tr=path.transmission==='airborne'?0.3:path.transmission==='contact'?0.2:0.15;if(Math.random()<tr*(1-(susceptible.phenotype?.immune_strength??0.5)*0.5)){if(!susceptible.infections)susceptible.infections=[];susceptible.infections.push({pathogen_id:pathId,days_remaining:path.duration_days,infected_day:simDay});return true;}return false;}
export function updateGutMicrobiome(individual,worldState){if(!individual.microbiome)individual.microbiome={diversity:0.5,composition:{}};const dd=worldState.food_abundance*0.5+(individual.inventory?.dried_food?0.1:0)+(individual.inventory?.food>3?0.2:0);individual.microbiome.diversity=individual.microbiome.diversity*0.95+dd*0.05;individual.phenotype.immune_strength=Math.min((individual.phenotype.immune_strength??0.5)*0.99+individual.microbiome.diversity*0.01,1.0);}
export function computeHealthStats(population){const living=population.filter(i=>!i.is_dead);if(living.length===0)return{sick_count:0,sick_rate:0,pathogen_diversity:0};const sick=living.filter(i=>i.infections?.length>0);return{sick_count:sick.length,sick_rate:sick.length/living.length,pathogen_diversity:new Set(sick.flatMap(i=>i.infections?.map(inf=>inf.pathogen_id)??[])).size};}
