[package]
name = "anatolia-sim-desktop"
version = "2.2.10"
edition = "2021"

[build-dependencies]
tauri-build = { version = "1" }

[dependencies]
serde_json = "1"
tauri = { version = "1", features = ["process-command-api", "updater"] }

